# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
corpus/**/screenshot.before-dismiss.jpg

corpus/**/render-canary.json

# Grind 0/2 transient survey outputs — research artifacts, not corpus goldens.
# Re-generera via scripts/drift-survey.ts resp. scripts/freeze-breadth.ts.
fixtures/drift-survey/**/page.mhtml
fixtures/drift-survey/**/page.pre-embed.mhtml
fixtures/drift-survey/**/screenshot.jpg
fixtures/breadth-50/**/page.mhtml
fixtures/breadth-50/**/page.pre-embed.mhtml
fixtures/breadth-50/**/screenshot.jpg

# angel breadth validation — transient scratch captures + report, never committed.
fixtures/angel-sample/

# Local secrets. The committed template is .env.example (names only, no values).
# Real keys (BROWSERBASE_API_KEY, etc.) live in .env locally / in the runtime
# environment — never committed.
.env
.env.*
!.env.example
