{
  "// varför": "scripts/ låg utanför ALL typkoll (granskningsfynd 2026-07-28) — nattloopen, preview-arbetaren, katalogkedjan och fleet-runnern kompilerades aldrig av tsc. Första körningen fångade fem skarpa buggar: cohorts/success-tappet (kohortvarianter föddes oscopade), validated.notes/plan.notes-fantomfälten, angel-reports --strict-ReferenceError och två tyst ignorerade Stagehand-timeouts. CI kör denna via `bun run typecheck:scripts`.",
  "extends": "./tsconfig.json",
  "include": ["scripts/**/*.ts", "scripts/types/*.d.ts"],
  "exclude": ["node_modules", "dist"],
  "compilerOptions": {
    "noEmit": true,
    "types": ["vite/client"]
  }
}
